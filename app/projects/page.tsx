import { PagePlaceholder } from "@/components/PagePlaceholder"

export const metadata = { title: "Projects — Juno" }

export default function ProjectsPage() {
  return (
    <PagePlaceholder kicker="Building" title="Projects">
      <p>Featured work — SmartCare GPS, Optibook, FoNE Research, EEG Hand Control — lands in Phase 5.</p>
    </PagePlaceholder>
  )
}
